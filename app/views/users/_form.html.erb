<%= simple_form_for(@users, :html => {:class => "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  	<%= f.input :name %>
  	<%= f.input :email %>
  	<%= f.input :company_id %>
  	<%= f.input :role %>

    <%= f.button :submit, :class => "btn btn-primary"%>
<% end %>
